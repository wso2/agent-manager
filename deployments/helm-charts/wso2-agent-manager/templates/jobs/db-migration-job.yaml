{{- if .Values.dbMigration.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: amp-db-migration
  labels:
    {{- include "agent-management-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: db-migration
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": before-hook-creation
spec:
  backoffLimit: {{ .Values.dbMigration.backoffLimit }}
  ttlSecondsAfterFinished: 300
  template:
    metadata:
      name: amp-db-migration
      labels:
        {{- include "agent-management-platform.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: db-migration
    spec:
      {{- include "agent-management-platform.imagePullSecrets" . | nindent 6 }}
      restartPolicy: Never
      {{- with .Values.dbMigration.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.postgresql.enabled }}
      initContainers:
        - name: wait-for-db
          image: postgres:16-alpine
          command:
            - sh
            - -c
            - |
              until pg_isready -h {{ include "agent-management-platform.postgresql.host" . }} -p {{ include "agent-management-platform.postgresql.port" . }} -U {{ include "agent-management-platform.postgresql.username" . }}; do
                echo "Waiting for PostgreSQL to be ready..."
                sleep 2
              done
              echo "PostgreSQL is ready!"
          env:
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "agent-management-platform.postgresql.secretName" . }}
                  key: {{ include "agent-management-platform.postgresql.secretPasswordKey" . }}

        # Generate builtin evaluators JSON using versioned SDK
        - name: generate-builtin-evaluators
          image: "{{ .Values.dbMigration.evaluatorGenerator.image.repository }}:{{ .Values.dbMigration.evaluatorGenerator.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.dbMigration.evaluatorGenerator.image.pullPolicy }}
          command:
            - python
            - -c
            - |
              import json
              from amp_evaluation.evaluators.builtin import list_builtin_evaluators

              evaluators = list_builtin_evaluators()
              with open('/data/builtin_evaluators.json', 'w') as f:
                  json.dump(evaluators, f, indent=2, default=str)

              print(f"✅ Generated {len(evaluators)} builtin evaluators")
          volumeMounts:
            - name: evaluators-data
              mountPath: /data
          {{- with .Values.dbMigration.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- end }}
      containers:
        - name: db-migration
          {{- with .Values.dbMigration.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .Values.dbMigration.image.repository }}:{{ .Values.dbMigration.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.dbMigration.image.pullPolicy }}
          command:
            - /go/bin/agent-manager-service
            - -migrate
            - -server=false
          env:
            - name: DB_HOST
              value: {{ include "agent-management-platform.postgresql.host" . | quote }}
            - name: DB_PORT
              value: {{ include "agent-management-platform.postgresql.port" . | quote }}
            - name: DB_NAME
              value: {{ include "agent-management-platform.postgresql.database" . | quote }}
            - name: DB_USER
              value: {{ include "agent-management-platform.postgresql.username" . | quote }}
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "agent-management-platform.postgresql.secretName" . }}
                  key: {{ include "agent-management-platform.postgresql.secretPasswordKey" . }}
            - name: API_KEY_VALUE
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.agentManagerService.config.apiKey.existingSecret | default (include "agent-management-platform.agentManagerService.fullname" .) }}
                  key: {{ .Values.agentManagerService.config.apiKey.existingSecretKey | default "api-key" }}
            - name: LOG_LEVEL
              value: "INFO"
            - name: OPEN_CHOREO_BASE_URL
              value: "http://openchoreo-api.openchoreo-control-plane:8080/api/v1"
            # Point to generated evaluators JSON
            - name: BUILTIN_EVALUATORS_PATH
              value: "/data/builtin_evaluators.json"
          volumeMounts:
            - name: evaluators-data
              mountPath: /data
              readOnly: true
          {{- with .Values.dbMigration.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      volumes:
        - name: evaluators-data
          emptyDir: {}
{{- end }}
