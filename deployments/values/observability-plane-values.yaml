# OpenChoreo Observability Plane Values
# Reference: https://openchoreo.dev/docs/getting-started/try-it-out/on-self-hosted-kubernetes/

# OpenSearch configuration
openSearch:
  enabled: true

openSearchCluster:
  enabled: false

# External Secrets Operator
external-secrets:
  enabled: false

# OIDC security configuration (Thunder integration)
security:
  oidc:
    # Thunder JWKS endpoint for token validation
    jwksUrl: "http://thunder-service.openchoreo-control-plane.svc.cluster.local:8090/oauth2/jwks"
