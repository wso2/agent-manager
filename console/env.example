# Environment variables for Agent Management Console
# Copy this file to .env and adjust values for your local setup:
#   cp env.example .env

# OAuth / Thunder (Asgardeo V2) — used by envsubst on config.template.js
AUTH_CLIENT_ID=amp-console-client
AUTH_BASE_URL=http://thunder.amp.localhost:8080
AUTH_SCOPES=openid profile email
# RFC 8707 resource indicator, sent on /oauth2/authorize. Set it to the amp
# resource server identifier ("urn:wso2:amp") to get a token that carries the
# amp: permission scopes; the token's aud becomes that identifier instead of
# AUTH_CLIENT_ID, so agent-manager-service's KEY_MANAGER_AUDIENCE must accept it.
# Leave empty to keep the pre-RBAC behaviour (aud stays the client id).
# Only useful alongside the matching amp: scopes in AUTH_SCOPES.
AUTH_RESOURCE=
SIGN_IN_REDIRECT_URL=http://localhost:3000/login
SIGN_OUT_REDIRECT_URL=http://localhost:3000/login
VALIDATE_ID_TOKEN=false
CLOCK_TOLERANCE=300

# API Configuration
API_BASE_URL=http://localhost:9000
GATEWAY_CONTROL_PLANE_URL=http://localhost:9243
GATEWAY_VERSION=v0.11.0

# Observability / Instrumentation
INSTRUMENTATION_URL=http://default-default.gateway.localhost:19080/otel

# Documentation base URL for in-product doc links. "latest" tracks the newest
# released docs; released charts pin this to their own documentation version.
DOCS_URL=https://wso2.github.io/agent-manager/docs/latest

# Request size limits (bytes). MAX_REQUEST_BODY_BYTES caps a save request so an
# oversized one is refused with a readable error rather than a WAF's opaque 403;
# 0 disables the check. FILE_MOUNT_MAX_FILE_BYTES caps a single file mount and
# should match agent-manager-service's value. Unset uses the defaults shown.
MAX_REQUEST_BODY_BYTES=57344
FILE_MOUNT_MAX_FILE_BYTES=1000000

# Guardrails Configuration
GUARDRAILS_CATALOG_URL=https://db720294-98fd-40f4-85a1-cc6a3b65bc9a-prod.e1-us-east-azure.choreoapis.dev/api-platform/policy-hub-api/policy-hub-public/v1.0/policies?categories=Guardrails,AI&limit=100
GUARDRAILS_DEFINITION_BASE_URL=https://db720294-98fd-40f4-85a1-cc6a3b65bc9a-prod.e1-us-east-azure.choreoapis.dev/api-platform/policy-hub-api/policy-hub-public/v1.0/policies
GUARDRAIL_CAP_AWS_BEDROCK=false
GUARDRAIL_CAP_AZURE_CONTENT_SAFETY=false
GUARDRAIL_CAP_GRANITE_GUARDIAN=false
GUARDRAIL_CAP_NEMO_GUARD=false
GUARDRAIL_CAP_SEMANTIC_GUARDRAILS=false

# Authentication
DISABLE_AUTH=false

# Feature flags
FEATURE_FLAG_ENABLE_PRIVATE_REPO_SUPPORT=false
FEATURE_FLAG_ENABLE_IDENTITY_PROVIDER_MANAGED_MODE=false
FEATURE_FLAG_ENABLE_PROFILE_MANAGEMENT=false
FEATURE_FLAG_ENABLE_USER_MANAGEMENT=false

